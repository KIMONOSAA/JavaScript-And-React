DOM 提供了不同类型的对象，以便表示文档中的不同部分。以下是一些常见的 DOM 类型：

文档节点（Document）：代表整个文档，是 DOM 层次结构的根节点。文档节点是其他节点的最上层父节点。

元素节点（Element）：表示 HTML 或 XML 文档中的元素，例如 <div>、<p> 或 <span>。元素节点具有标签名称、属性和子节点。

文本节点（Text）：表示元素节点中的文本内容。例如，<p>Hello, world!</p> 中的 "Hello, world!" 是一个文本节点。

属性节点（Attribute）：表示元素节点的属性。每个元素节点可以具有零个或多个属性节点，用于描述元素的特性。

注释节点（Comment）：表示文档中的注释内容。例如，<!-- 这是一个注释 --> 是一个注释节点。

文档片段节点（DocumentFragment）：表示文档的一个部分或一个临时容器。它可以包含多个子节点，类似于一个轻量级的文档。

这些节点类型构成了 DOM 树的节点层次结构，其中父节点包含子节点，子节点又可以作为父节点包含其他子节点。通过操作这些节点对象，可以实现对文档结构的访问、修改和创建。